{
  "name": "@pantheon/game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "export-terrain-seed": "../../packages/assets/node_modules/.bin/tsx scripts/export-terrain-seed.ts",
    "seed-terrain": "../../packages/assets/node_modules/.bin/tsx scripts/seed-terrain.ts",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "viem": "^2.35.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
